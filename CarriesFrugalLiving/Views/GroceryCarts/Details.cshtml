@model CarriesFrugalLiving.Models.GroceryCart

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>GroceryCart</h4>
    <hr />
    <dl class="dl-horizontal">


        <dt>
            @Html.DisplayNameFor(model => model.CreateDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreateDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>

<div>
    @Html.ActionLink("Add", "AddGroceryItem", new { id = Model.ID })
    <hr />
    <div id="list">
        @Html.Action("GCartItemList", "GroceryCarts", new { id = Model.ID })
    </div>

</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |

    @Html.ActionLink("My Grocery List", "MyGroceryList", new { id = Model.ID }) |

    @Html.ActionLink("Back to List", "Index")
</p>

<script type="text/javascript">

        function d(id) {
            var url = "/GroceryCarts/DeleteCartItem/";
            $.get(url, { id: id })
             .done(function (response) {
                 $("#list").html(response);
             });
        }
</script>